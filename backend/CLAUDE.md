# Backend Development Guide

## Quick Start
```bash
uv sync                              # Install dependencies
uv run fastapi dev backend/main.py   # Start dev server (port 8000)
uv run pytest -v                     # Run tests
uv run ruff check .                  # Lint
uv run ruff format .                 # Format
uv run mypy .                        # Type check
```

## Structure
See main CLAUDE.md for full project structure.
This file will be expanded as backend development begins in Phase 1.
